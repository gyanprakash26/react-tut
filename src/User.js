import React from "react";
// this is function component
// basic thing is that func name and file name should be same
// also must have return type and in the return have only one tag or one section
export default function User() {
  return (
    <div>
      <h1>Function component</h1>
      <p> this is the function component</p>
      <hr></hr>
    </div>
  );
}
// export default User;  we can also use this method for export file

